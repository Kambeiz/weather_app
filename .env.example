# Server Configuration
PORT=3000
NODE_ENV=development

# Session Secret (generate a strong secret for production)
SESSION_SECRET=your-session-secret-key

# OpenWeatherMap API Key (get one at https://openweathermap.org/api)
OPENWEATHER_API_KEY=your-openweather-api-key

# WeatherAPI Key (get one at https://www.weatherapi.com/)
WEATHERAPI_KEY=your-weatherapi-key-here

# Keys for the environment and sending mail (local test)
RESEND_API_KEY=your-resend-api-key
EMAIL_FROM=your-verified-email@resend.dev

# Mailjet SMTP Configuration (primary for production)
EMAIL_HOST=in-v3.mailjet.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-mailjet-api-key
EMAIL_PASS=your-mailjet-secret-key
EMAIL_FROM=d.weathertheapp@gmail.com

# Database Configuration
# Choose one: SQLite (local development) or MySQL (production)

# SQLite (local development - comment out when using MySQL)
# DB_PATH=./database/weather.db

# PostgreSQL/Supabase (for big production - comment when using SQLite)
#DATABASE_URL=postgresql://postgres:your-password@your-host:5432/postgres
#SUPABASE_URL=https://your-project-id.supabase.co
#SUPABASE_ANON_KEY=your-anon-key
#SUPABASE_SERVICE_KEY=your-service-role-key
#POSTGRES_PASSWORD=yourdatabasepassword

# MySQL (production - Aiven)
DB_HOST=db_adress.xyz
DB_USER=nameuser
DB_PASSWORD=yourpassword
DB_NAME=defaultdb
DB_PORT=123456
